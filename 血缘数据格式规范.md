# 血缘数据 JSON 格式规范

> 版本：1.0
> 日期：2026-08-03
> 维护方：数分数仓团队
> 血缘来源：外部项目解析后上传

---

## 一、文件组织

```
lineage/
├── cust_lineage.json       # 消费者主题血缘
├── sale_lineage.json       # 交易主题血缘
├── prod_lineage.json       # 商品主题血缘
├── mall_lineage.json       # 商场主题血缘
├── mkt_lineage.json        # 营销主题血缘
├── cont_lineage.json       # 合同主题血缘
├── fin_lineage.json        # 财务主题血缘
├── hr_lineage.json         # 人资主题血缘
├── ptnr_lineage.json       # 商户主题血缘
├── traf_lineage.json       # 流量主题血缘
├── base_lineage.json       # 基础主题血缘
├── fund_lineage.json       # 资金主题血缘
├── wk_lineage.json         # 流程主题血缘
└── shop_lineage.json       # 店铺主题血缘
```

**命名规则**：`{domain_code}_lineage.json`

---

## 二、JSON 结构定义

### 2.1 文件顶层结构

```json
{
  "domain": "cust",
  "domain_name": "消费者",
  "version": "1.0.0",
  "parsed_at": "2026-08-03T10:00:00+08:00",
  "source_project": "data-lineage-parser",
  "lineages": [ ... ]
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| domain | string | 是 | 主题域代码，如 cust |
| domain_name | string | 是 | 主题域中文名 |
| version | string | 是 | 血缘数据版本号 |
| parsed_at | string | 是 | 解析时间，ISO 8601 格式 |
| source_project | string | 是 | 来源项目名称 |
| lineages | array | 是 | 血缘关系列表 |

### 2.2 单条血缘关系结构

```json
{
  "lineage_id": "LN_CUST_001",
  "target_table": "dwd_cust_customer_df",
  "target_table_cn": "消费者明细表",
  "target_layer": "dwd",
  "source_tables": [
    {
      "table_name": "ods_cust_member_info",
      "table_cn": "会员信息表",
      "source_layer": "ods",
      "source_system": "CRM"
    },
    {
      "table_name": "ods_cust_register_log",
      "table_cn": "注册日志表",
      "source_layer": "ods",
      "source_system": "APP"
    }
  ],
  "column_mappings": [
    {
      "target_column": "customer_id",
      "source_columns": [
        {
          "table": "ods_cust_member_info",
          "column": "member_id"
        },
        {
          "table": "ods_cust_register_log",
          "column": "user_id"
        }
      ],
      "transform_type": "coalesce",
      "transform_logic": "优先取 member_id，为空取 user_id"
    },
    {
      "target_column": "customer_name",
      "source_columns": [
        {
          "table": "ods_cust_member_info",
          "column": "nick_name"
        }
      ],
      "transform_type": "direct",
      "transform_logic": "直接映射"
    }
  ],
  "related_metrics": ["M_CUST_001", "M_CUST_003"]
}
```

### 2.3 字段说明

#### 血缘关系主表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| lineage_id | string | 是 | 血缘ID，格式 LN_{DOMAIN}_{序号} |
| target_table | string | 是 | 目标表名 |
| target_table_cn | string | 否 | 目标表中文名 |
| target_layer | string | 是 | 目标表所属层级（ods/dwd/dws/ads/dim） |
| source_tables | array | 是 | 源表列表 |
| column_mappings | array | 否 | 字段级映射（V1可填表级，V1.5补字段级） |
| related_metrics | array | 否 | 关联的指标ID列表 |

#### 源表

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| table_name | string | 是 | 源表名 |
| table_cn | string | 否 | 源表中文名 |
| source_layer | string | 是 | 源表层级 |
| source_system | string | 否 | 源系统名称（CRM/ERP/APP等） |

#### 字段映射

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| target_column | string | 是 | 目标字段名 |
| source_columns | array | 是 | 源字段列表 |
| transform_type | string | 是 | 转换类型（见下方枚举） |
| transform_logic | string | 否 | 转换逻辑描述 |

**transform_type 枚举值：**

| 值 | 说明 | 示例 |
|----|------|------|
| direct | 直接映射，无转换 | A.col → B.col |
| coalesce | 多源取值，优先级 | 优先取A，为空取B |
| concat | 拼接 | A.col + B.col |
| calc | 计算 | A.col * B.col |
| aggregate | 聚合 | sum(A.col) |
| lookup | 查表关联 | join dim_table |
| custom | 自定义逻辑 | 见 transform_logic 描述 |

---

## 三、完整示例文件

以下为 `cust_lineage.json` 的完整示例：

```json
{
  "domain": "cust",
  "domain_name": "消费者",
  "version": "1.0.0",
  "parsed_at": "2026-08-03T10:00:00+08:00",
  "source_project": "data-lineage-parser",
  "lineages": [
    {
      "lineage_id": "LN_CUST_001",
      "target_table": "dwd_cust_customer_df",
      "target_table_cn": "消费者明细表",
      "target_layer": "dwd",
      "source_tables": [
        {
          "table_name": "ods_cust_member_info",
          "table_cn": "会员信息表",
          "source_layer": "ods",
          "source_system": "CRM"
        },
        {
          "table_name": "ods_cust_register_log",
          "table_cn": "注册日志表",
          "source_layer": "ods",
          "source_system": "APP"
        }
      ],
      "column_mappings": [
        {
          "target_column": "customer_id",
          "source_columns": [
            {"table": "ods_cust_member_info", "column": "member_id"},
            {"table": "ods_cust_register_log", "column": "user_id"}
          ],
          "transform_type": "coalesce",
          "transform_logic": "优先取 member_id，为空取 user_id"
        },
        {
          "target_column": "customer_name",
          "source_columns": [
            {"table": "ods_cust_member_info", "column": "nick_name"}
          ],
          "transform_type": "direct",
          "transform_logic": "直接映射"
        },
        {
          "target_column": "register_time",
          "source_columns": [
            {"table": "ods_cust_register_log", "column": "create_time"}
          ],
          "transform_type": "direct",
          "transform_logic": "直接映射"
        }
      ],
      "related_metrics": ["M_CUST_001", "M_CUST_003"]
    },
    {
      "lineage_id": "LN_CUST_002",
      "target_table": "dws_cust_member_summary_dm",
      "target_table_cn": "会员汇总日表",
      "target_layer": "dws",
      "source_tables": [
        {
          "table_name": "dwd_cust_customer_df",
          "table_cn": "消费者明细表",
          "source_layer": "dwd",
          "source_system": "数仓"
        }
      ],
      "column_mappings": [
        {
          "target_column": "daily_active_cnt",
          "source_columns": [
            {"table": "dwd_cust_customer_df", "column": "customer_id"}
          ],
          "transform_type": "aggregate",
          "transform_logic": "count(distinct customer_id) where is_active=1"
        }
      ],
      "related_metrics": ["M_CUST_002"]
    }
  ]
}
```

---

## 四、上传约定

1. **文件命名**：`{domain_code}_lineage.json`，如 `cust_lineage.json`
2. **上传目录**：`lineage/`
3. **更新方式**：整体覆盖（每次上传完整文件，不做增量）
4. **更新频率**：按需，建议每月或每次表结构变更后更新
5. **校验规则**：
   - JSON 格式合法
   - domain 字段与文件名一致
   - lineage_id 不重复
   - target_table 不为空
   - source_tables 至少 1 条

---

## 五、版本演进

| 版本 | 能力 | 说明 |
|------|------|------|
| 1.0 | 表级 + 字段级血缘 | 当前版本，手动/外部解析上传 |
| 1.5 | 血缘变更追踪 | 记录血缘变更历史 |
| 2.0 | 自动解析 | 平台自动解析 SQL 生成血缘 |
