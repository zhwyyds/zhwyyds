# 数据治理项目 — 优化建议报告（项目管理视角）

> 编制：2026-08-05 ｜ 视角：高级项目经理
> 输入：数据治理项目方案_v2.md、验收评分标准.md、功能缺失审查报告.md、代码与配置现状
> 定位：与《功能缺失审查报告.md》（技术缺口）互补，聚焦**管理、流程、风险、交付**侧

---

## 一、验收就绪度：最大风险是「数据没到位」

对标《验收评分标准.md》6 维度（词根覆盖 20 / 命名 20 / 同名同义 25 / 口径 15 / 血缘 10 / 模型评审 10）：

| 验收项 | 要求 | 现状 | 差距 |
|---|---|---|---|
| 词根入库率 | ≥95% 指标有关联词根 | 仅 3/14 域有词根，根数个位 | **严重** |
| 指标规模 | ~2000 条 | 4 条（仅 sale） | **严重** |
| 血缘覆盖 | 14 域 lineage 文件 | 1 域（sale） | **严重** |
| 命名/拼音 | 英文命名率≥95%、拼音清除率≥90% | 无法在规模上验证 | 待数据 |

**管理结论**：平台功能再完善，验收也过不了——缺的是**数据供给计划**。建议立即明确：
1. 谁负责把 14 域原始指标/词根灌入平台（来源：多源系统归集结果）？
2. 按域排期：每域的词根 → 指标 → 评审 → 评分 → 发布的全流程里程碑；
3. 明确「验收日」与「数据到位日」两个关键节点。

---

## 二、治理流程与责任缺失

### 2.1 责任矩阵为空
- `config/domains.csv` 的 **`owner` 字段 14 行全空**——14 个主题域没有一位数据责任人。
- 数据治理没人「认领」，一切检查项都无处落地。**建议 1 周内补全 owner**，并建立"域主负责制"。

### 2.2 无数据供给/导入流程
- 多源系统归集后的数据如何进入平台：无导入脚本、无格式校验、无转换说明。
- 建议沉淀一份《数据导入规范》：CSV/JSON 格式校验工具 + 导入记录 + 出错回滚。

### 2.3 无治理质量看板
- 验收标准里有黄灯/红灯阈值，但平台缺乏「域级治理红绿灯」总览（每域：词根数/指标数/评分分布/血缘覆盖率）。

---

## 三、运维与数据安全（本地化运行更要补）

| # | 缺口 | 风险 | 建议 |
|---|---|---|---|
| 1 | **无备份方案** | CSV/JSON 是全库，被误改/损坏即丢 | 每日自动快照（`cp -r` 到 `backup/`）+ 保留 N 天；首次提交已入库，可 git 兜底 |
| 2 | **无数据自检** | 手改 CSV 格式错乱、编码损坏难发现 | 写 `validate` 命令：字段数/必填/编码/引用完整性校验 |
| 3 | **无操作审计** | 谁改了指标、谁发布了、谁重评了——无痕迹 | 发布/评审/评分写审计日志（已有 version_history，扩展到全动作） |
| 4 | **无故障排查手册** | 服务起不来/API Key 失效时只能翻代码 | 《运维手册》：启动检查、常见错误、日志位置 |
| 5 | **密钥管理** | `.env`/`secrets.json` 明文 | 已 gitignore，但需固定文件权限 + 定期轮换 |

---

## 四、工程与质量（PM 视角的底线）

1. **Task #17 长期挂着**：scoring/release 零测试、CLI 0% 覆盖 → 这周必须收掉，否则新功能全是裸奔。
2. **无 CI**：代码已上 GitHub，补 Actions（ruff + mypy + pytest + coverage）成本极低、收益高。
3. **前端零自动化测试**：纯手工验证；至少补一个 smoke 级 E2E（起服务 → 打 10 个核心 API）。
4. **2000 指标规模未做性能验证**：CSV 全量扫描 + 每指标评分文件，需压测基准（如 10 万行 CSV 的加载耗时），必要时分批处理。
5. **两套评分口径**（指标级 scoring / 项目级 acceptance）不统一 → **PM 拍板**：验收对外用哪套，或做映射，避免"两个分都对不上"。

---

## 五、项目治理与交付物

### 5.1 迭代与排期缺失
- 剩余工作（功能审查 P0–P3 + 本报告）没有进入排期。建议立即排三迭代：
  - **迭代 1（本周）**：Task #17 收尾 + 数据自检命令 + 备份脚本 + CI
  - **迭代 2**：P0 批量生成后端 + 词根/血缘写接口
  - **迭代 3**：P1 发布门禁/撤销 + 域级看板 + 验收演示数据
- 用 WorkBuddy 项目功能承接需求/缺陷/验收清单，PM 每周过一遍。

### 5.2 演示与验收准备
- 当前 4 条指标无法支撑演示效果。建议做一套**黄金演示数据**：覆盖 S/A/B/C 四级、含同名异义/拼音残留/口径空三类反例，一次讲清"评审→评分→发布"全链路。

### 5.3 文档体系
- 9 个根目录 md 各自为政，无索引、无版本、无变更记录。建议：
  - 建 `docs/README.md` 总索引（设计/规范/报告/手册四类）；
  - 新增 `CHANGELOG.md`，从今日起记录每次变更；
  - 补《操作手册》（面向数据治理人员，非开发者）。

---

## 六、风险登记（Top 5）

| 风险 | 等级 | 缓解措施 |
|---|---|---|
| 数据供给不到位，验收失败 | 高 | 上数据供给计划 + 责任到人 + 里程碑 |
| 单机文件存储数据丢失 | 高 | 每日备份 + git 兜底 + 恢复演练 |
| 大模型 API 依赖（成本/可用性/Key 失效） | 中 | mock/live 双模已备；限流与失败重试；评估国产模型替代 |
| 14 域责任真空 | 中 | 补 owner + 域主负责制 |
| 两套评分口径争议 | 中 | PM 拍板统一口径或映射表 |

---

## 七、优先级总览（管理动作）

| 优先级 | 动作 | 责任建议 |
|---|---|---|
| 立刻 | 补 domains owner；排三迭代；定验收口径 | PM + 数据负责人 |
| 本周 | Task #17 收尾；备份脚本；数据自检命令；CI | 开发 |
| 迭代 2 | P0 批量生成 + 词根/血缘写接口 | 开发 |
| 迭代 3 | 发布门禁/撤销；域级看板；黄金演示数据；文档体系 | 开发 + PM |

---
*本报告与《功能缺失审查报告.md》配套：前者治「功能有没有」，本文治「项目成不成」。*
