# 大模型集成方案 — 词根生成与指标评审

> 版本：1.0
> 日期：2026-08-03
> 核心策略：多模型并行 → 比对择优 → 人工确认

---

## 一、总体架构

```
                    ┌─────────────┐
                    │  输入数据     │
                    │ (中文术语/   │
                    │  指标定义)    │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ Model A  │ │ Model B  │ │ Model C  │
        │ (GPT)    │ │ (Claude) │ │ (千问)   │
        └────┬─────┘ └────┬─────┘ └────┬─────┘
             │             │            │
             ▼             ▼            ▼
        ┌──────────────────────────────────────┐
        │          结果比对引擎                  │
        │  · 一致性检查（完全一致→直接通过）      │
        │  · 差异性分析（不一致→标记差异点）      │
        │  · 优先级排序（按规则选最优）           │
        └──────────────┬───────────────────────┘
                       │
              ┌────────┴────────┐
              ▼                 ▼
        ┌──────────┐      ┌──────────┐
        │ 一致通过  │      │ 存在分歧  │
        │ → 自动入库 │      │ → 人工确认 │
        └──────────┘      └──────────┘
```

---

## 二、场景一：词根生成

### 2.1 输入

```json
{
  "input_type": "root_generation",
  "domain": "cust",
  "terms": [
    {"cn_term": "客户", "context": "家居卖场的消费者，包括会员和非会员"},
    {"cn_term": "会员等级", "context": "根据消费金额和频次划分的会员等级"},
    {"cn_term": "到店次数", "context": "消费者在一定周期内到访卖场的次数"}
  ]
}
```

### 2.2 Prompt 模板（发给每个模型）

```
你是一位数据治理专家，精通英文命名规范。请将以下中文术语翻译为标准化的英文词根。

要求：
1. 给出英文全称（root_en）：使用标准英文单词，不用拼音
2. 给出英文缩写（root_abbr）：不超过6个字符，遵循以下规则：
   - 单词取前3-4个字母（如 customer → cust）
   - 短词直接用全称（如 id, name）
   - 多词组合取首字母或缩写拼接
3. 给出词根类型（root_type）：noun/verb/adj/unit/time
4. 给出简短说明

术语列表：
{terms_json}

请以 JSON 数组格式输出，每个术语包含：cn_term, root_en, root_abbr, root_type, description
```

### 2.3 多模型比对规则

| 比对项 | 规则 | 权重 |
|--------|------|------|
| root_en 一致性 | 多模型英文全称是否一致 | 40% |
| root_abbr 一致性 | 多模型缩写是否一致 | 30% |
| root_type 一致性 | 多模型词根类型是否一致 | 10% |
| 命名规范度 | 是否符合缩写规则（短词全拼、长词截取） | 20% |

### 2.4 择优逻辑

```
情况1：所有模型结果完全一致
  → 自动通过，source_model = "model_consensus"
  → review_status = "approved"

情况2：多数一致（≥ 2/3 一致）
  → 采用多数结果
  → source_model = "model_majority"
  → review_status = "approved"
  → 记录少数派意见到评审记录

情况3：结果分歧较大（无多数）
  → 标记为待人工确认
  → source_model = "model_conflict"
  → review_status = "pending"
  → 生成比对报告供人工决策
```

### 2.5 评审记录格式

文件路径：`reviews/root_reviews/{domain}_root_review_{batch}.json`

```json
{
  "review_id": "RR_CUST_001",
  "domain": "cust",
  "review_type": "root_generation",
  "created_at": "2026-08-03T10:00:00+08:00",
  "models_used": ["gpt-4o", "claude-3.5-sonnet", "glm-4"],
  "items": [
    {
      "cn_term": "客户",
      "context": "家居卖场的消费者",
      "model_results": [
        {
          "model": "gpt-4o",
          "root_en": "customer",
          "root_abbr": "cust",
          "root_type": "noun",
          "description": "客户的统称"
        },
        {
          "model": "claude-3.5-sonnet",
          "root_en": "customer",
          "root_abbr": "cust",
          "root_type": "noun",
          "description": "消费者客户"
        },
        {
          "model": "glm-4",
          "root_en": "customer",
          "root_abbr": "cst",
          "root_type": "noun",
          "description": "客户"
        }
      ],
      "comparison": {
        "root_en_consistent": true,
        "root_abbr_consistent": false,
        "root_type_consistent": true,
        "conflict_fields": ["root_abbr"]
      },
      "final_decision": {
        "root_en": "customer",
        "root_abbr": "cust",
        "root_type": "noun",
        "description": "客户的统称",
        "decision_reason": "GPT和Claude一致使用cust，符合前4字母截取规则",
        "decision_type": "model_majority"
      },
      "auto_approved": true
    }
  ]
}
```

---

## 三、场景二：指标评审

### 3.1 输入

```json
{
  "input_type": "metric_review",
  "domain": "sale",
  "metrics": [
    {
      "metric_id": "M_SALE_001",
      "metric_cn": "月度销售额",
      "metric_en": "monthly_sales_amt",
      "caliber_desc": "自然月内已完成订单的销售总金额，不含退款",
      "root_ids": ["R_SALE_001", "R_TIME_001"],
      "unit": "元",
      "frequency": "月"
    }
  ]
}
```

### 3.2 Prompt 模板

```
你是一位数据治理评审专家。请对以下指标定义进行评审，检查以下方面：

1. 命名规范性：metric_en 是否由标准词根组合而成，是否符合英文命名规范
2. 口径清晰度：caliber_desc 是否清晰、无歧义，是否包含业务定义和计算逻辑
3. 同名同义风险：该指标名称是否可能与其他指标产生同名异义或同义异名
4. 词根关联性：root_ids 关联的词根是否能正确还原为 metric_en
5. 单位与频率：unit 和 frequency 是否合理

指标列表：
{metrics_json}

请以 JSON 数组格式输出评审结果，每个指标包含：
metric_id, naming_score(1-5), naming_issues, caliber_score(1-5), 
caliber_issues, conflict_risks, root_match(true/false), suggestions
```

### 3.3 多模型比对规则

| 比对项 | 规则 |
|--------|------|
| 评分一致性 | 多模型对同一指标的评分差异 ≤ 1 分 → 可信 |
| 问题识别 | ≥ 2 个模型提出相同问题 → 确认问题存在 |
| 冲突风险 | 任意模型提出同名同义风险 → 必须人工复核 |

### 3.4 择优逻辑

```
情况1：所有模型评分均 ≥ 4，无冲突风险
  → 自动通过
  → review_status = "approved"

情况2：有模型评分 < 4，但无冲突风险
  → 标记问题项
  → review_status = "pending"
  → 附带整改建议

情况3：任意模型提出冲突风险
  → 强制人工复核
  → review_status = "pending"
  → 生成冲突报告
```

### 3.5 评审记录格式

文件路径：`reviews/metric_reviews/{domain}_metric_review_{batch}.json`

```json
{
  "review_id": "MR_SALE_001",
  "domain": "sale",
  "review_type": "metric_review",
  "created_at": "2026-08-03T10:00:00+08:00",
  "models_used": ["gpt-4o", "claude-3.5-sonnet", "glm-4"],
  "items": [
    {
      "metric_id": "M_SALE_001",
      "metric_en": "monthly_sales_amt",
      "model_reviews": [
        {
          "model": "gpt-4o",
          "naming_score": 5,
          "naming_issues": [],
          "caliber_score": 4,
          "caliber_issues": ["口径未明确是否含税"],
          "conflict_risks": [],
          "root_match": true,
          "suggestions": "建议口径中补充是否含税说明"
        },
        {
          "model": "claude-3.5-sonnet",
          "naming_score": 5,
          "naming_issues": [],
          "caliber_score": 4,
          "caliber_issues": ["口径未明确是否含税"],
          "conflict_risks": [],
          "root_match": true,
          "suggestions": "建议补充含税/不含税说明"
        },
        {
          "model": "glm-4",
          "naming_score": 4,
          "naming_issues": ["建议使用 monthly_sales_amount 更完整"],
          "caliber_score": 3,
          "caliber_issues": ["口径未明确含税、未明确统计时区"],
          "conflict_risks": [],
          "root_match": true,
          "suggestions": "建议口径补充含税说明和统计时区"
        }
      ],
      "comparison": {
        "naming_score_avg": 4.67,
        "caliber_score_avg": 3.67,
        "consistent_issues": ["口径未明确是否含税"],
        "conflict_detected": false
      },
      "final_decision": {
        "approved": false,
        "decision_type": "needs_revision",
        "action_items": [
          "口径中补充是否含税说明",
          "考虑统计时区说明"
        ],
        "review_status": "pending"
      }
    }
  ]
}
```

---

## 四、模型配置

文件路径：`config/models.csv`

```csv
model_id,model_name,provider,use_case,priority,enabled,api_endpoint,api_key_env,remark
1,gpt-4o,OpenAI,root_generation,1,true,,OPENAI_API_KEY,
2,claude-3.5-sonnet,Anthropic,root_generation,2,true,,ANTHROPIC_API_KEY,
3,qwen-plus,Qwen,root_generation,3,true,,DASHSCOPE_API_KEY,通义千问
4,gpt-4o,OpenAI,metric_review,1,true,,OPENAI_API_KEY,
5,claude-3.5-sonnet,Anthropic,metric_review,2,true,,ANTHROPIC_API_KEY,
6,qwen-plus,Qwen,metric_review,3,true,,DASHSCOPE_API_KEY,通义千问
```

**配置说明：**
- `api_key_env`：密钥所在环境变量名（**不在 CSV 里写密钥**）；启动时从进程环境、项目根 `.env` 或 `config/secrets.json` 注入（见 `config/secrets.example.json` / `.env.example`，**在等号或 JSON 值后面自行补充**）
- `priority`：调用优先级，数字小的先调
- `use_case`：root_generation / metric_review
- `enabled`：是否启用，可灵活增减模型
- 最少启用 2 个模型，建议 3 个

**M3 代码落地（`src/data_governance/llm/`）：**

| 项 | 说明 |
|----|------|
| `DATA_GOV_LLM_MODE` | `mock` / `live` / `auto`（默认） |
| Key | 环境变量 / `config/secrets.json` / `.env`（`api_key_env` 列指向变量名，**值本地补充**） |
| Pipeline | `RootGenerationPipeline` / `MetricReviewPipeline`：`use_mock=None` 时按 env 解析；Live 走 `build_live_clients` + `run_models_parallel_prompt` |
| CLI | `root generate` / `metric review` 支持 `--live` |
| API | `GET /api/llm/status`；`POST /api/metrics/{id}/review` 随 env 选 Mock/Live |
| 未做 | Live E2E 仅本地联调（缓存见 `.cache/llm/`，默认 7 天） |

---

## 五、执行流程

### 5.1 词根生成流程

```
1. 业务人员提供中文术语列表（Excel/CSV）
2. 系统按主题域读取术语，批量调用 N 个模型
3. 比对引擎对多模型结果进行一致性分析
4. 一致 → 自动入库；分歧 → 生成评审记录待人工确认
5. 人工确认后更新 roots/*.csv，review_status → approved
```

### 5.2 指标评审流程

```
1. 指标定义录入（含 metric_en + caliber_desc + root_ids）
2. 系统批量调用 N 个模型进行评审
3. 比对引擎汇总评审结果
4. 全部通过 → approved；有问题 → pending + 整改建议
5. 业务人员根据建议修改后重新提交评审
```

---

## 六、异常处理

| 异常 | 处理方式 |
|------|----------|
| 某个模型 API 超时/不可用 | 跳过该模型，用剩余模型结果比对（最少 2 个） |
| 所有模型都超时 | 标记为 pending，转为人工处理 |
| 模型返回格式异常 | 重试 1 次；仍异常则跳过该模型 |
| 比对结果无法自动决策 | 转人工确认，生成比对报告 |

---

## 七、成本控制

| 措施 | 说明 |
|------|------|
| 批量调用 | 一次请求处理多个术语/指标，减少 API 调用次数 |
| 缓存机制 | 相同输入不重复调用，缓存结果 7 天 |
| 模型分级 | 词根生成用中等模型，指标评审用强模型 |
| 增量评审 | 只评审新增/修改的指标，不全量重审 |
