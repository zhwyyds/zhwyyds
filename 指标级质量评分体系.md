# 指标级质量评分体系

> 版本：1.0
> 日期：2026-08-03
> 定位：**单个指标**的质量评分（区别于项目级验收评分）
> 核心要求：查看指标详情时，评分规则和打分明细可见

---

## 一、设计目标

### 1.1 为什么需要指标级评分

| 场景 | 项目级验收评分 | 指标级质量评分 |
|------|---------------|---------------|
| 看什么 | 整个项目治理到什么程度 | 这个指标本身合不合格 |
| 给谁看 | 项目负责人、管理层 | 指标负责人、数据开发 |
| 怎么用 | 决定项目是否验收通过 | 决定单个指标能否上线、要不要整改 |
| 粒度 | 14个维度聚合到100分 | 每个指标独立打分到100分 |

### 1.2 核心体验

```
用户点开一个指标详情 →

┌─────────────────────────────────────────────────────────┐
│  M_SALE_002  本月订单金额  monthly_order_amount           │
│  ─────────────────────────────────────────────────────  │
│  质量评分：85 / 100   等级：A ✅                          │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  评分明细                                           │  │
│  │                                                    │  │
│  │  维度          得分   满分   状态    扣分原因       │  │
│  │  ──────────── ───── ───── ─────── ────────────── │  │
│  │  命名规范      20     20    ✅      —              │  │
│  │  词根关联      15     15    ✅      —              │  │
│  │  口径完整      20     25    ⚠️      缺少统计周期    │  │
│  │  同名同义      20     20    ✅      —              │  │
│  │  血缘可查      10     10    ✅      —              │  │
│  │  模型评审      0      10    ❌      未经模型评审    │  │
│  │  ──────────── ───── ─────                          │  │
│  │  合计          85    100   A                       │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  待整改项                                           │  │
│  │  1. [口径] 补充统计周期描述（当前缺少"按自然月统计"）│  │
│  │  2. [评审] 提交大模型评审（当前 source=manual）     │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 二、评分维度与规则

### 2.1 六大维度总览

| 维度 | 满分 | 核心检查点 | 一票否决 |
|------|------|-----------|----------|
| 命名规范 | 20 | 英文名标准化、缩写可拆解、无拼音 | 有拼音残留 → 该维度直接 0 分 |
| 词根关联 | 15 | 关联词根非空、词根存在、词根可逆推 | — |
| 口径完整 | 25 | 三要素齐全（定义+公式+周期） | 口径为空 → 该维度直接 0 分 |
| 同名同义 | 20 | 无同名异义、无同义异名 | 同名异义 → 该维度直接 0 分 |
| 血缘可查 | 10 | 血缘ID非空、血缘记录存在 | — |
| 模型评审 | 10 | 经过模型评审、评审记录完整 | — |
| **合计** | **100** | | |

### 2.2 维度一：命名规范（20分）

| 检查项 | 分值 | 规则 | 扣分原因示例 |
|--------|------|------|-------------|
| 英文名标准化 | 8 | metric_en 为纯英文、小写下划线分隔、符合 `prefix_root_suffix` 格式 | "yuedingdanbijine" 拼音 → 扣 8 分 |
| 缩写可拆解 | 6 | metric_abbr 可拆解为已注册 root_abbr 的组合 | "mon_sal" 缺 "amt" 后缀 → 扣 3 分 |
| 无拼音残留 | 4 | metric_en / metric_abbr 中不含拼音片段 | "ord_jine" 含拼音 "jine" → 扣 4 分（该维度归零） |
| 命名格式一致 | 2 | 符合统一命名格式正则 `^[a-z]+(_[a-z]+)+$` | "orderAmount" 驼峰命名 → 扣 2 分 |

**评分逻辑：**
```
命名规范得分 = 8×(英文名标准化?1:0) + 6×缩写评分 + 4×(无拼音?1:0) + 2×(格式一致?1:0)

缩写评分：
  - 完全可拆解为 root_abbr 组合 → 6 分
  - 部分可拆解（≥50% token 匹配） → 3 分
  - 无法拆解 → 0 分

特殊规则：
  - 检测到拼音残留 → 整个维度 0 分（因为拼音是项目核心治理对象）
```

**检查方式：**
```python
# 拼音检测：将 metric_en 按 _ 拆分，每个 token 检查是否在拼音字典中
import pypinyin
def has_pinyin(metric_en):
    tokens = metric_en.split('_')
    for token in tokens:
        # 如果 token 不在英文词根库中，且能被识别为拼音 → 拼音残留
        if token not in root_en_set and is_valid_pinyin(token):
            return True, token  # 返回拼音片段
    return False, None

# 缩写拆解：metric_abbr 按 _ 拆分，每个 token 在 root_abbr 集合中匹配
def check_abbr_decomposition(metric_abbr, root_abbr_set):
    tokens = metric_abbr.split('_')
    matched = sum(1 for t in tokens if t in root_abbr_set)
    ratio = matched / len(tokens) if tokens else 0
    if ratio == 1.0:
        return 6, "完全可拆解"
    elif ratio >= 0.5:
        return 3, f"部分可拆解({matched}/{len(tokens)})"
    else:
        return 0, "无法拆解"
```

### 2.3 维度二：词根关联（15分）

| 检查项 | 分值 | 规则 | 扣分原因示例 |
|--------|------|------|-------------|
| 词根非空 | 5 | root_ids 字段非空 | 空 → 扣 5 分 |
| 词根存在 | 5 | root_ids 中的每个 ID 在 roots/*.csv 中存在 | "R_SALE_999" 不存在 → 扣 5 分 |
| 词根可逆推 | 5 | metric_en 可由 root_en 组合还原 | 英文名含未注册词根 → 扣 5 分 |

**评分逻辑：**
```
词根关联得分 = 5×(root_ids非空?1:0) 
             + 5×(所有root_id存在?1:0) 
             + 5×(英文名可逆推?1:0)

词根可逆推检查：
  1. 取 root_ids 对应的 root_en 值
  2. 检查 metric_en 是否等于这些 root_en 的某种组合（顺序无关，允许插入修饰词）
  3. 例如：metric_en = "monthly_order_amount"
     root_en = ["order", "amount"]
     → "monthly" 是时间修饰词，"order_amount" 由 root 组合 → ✅ 可逆推
```

### 2.4 维度三：口径完整（25分）— 最重要维度

| 检查项 | 分值 | 规则 | 扣分原因 |
|--------|------|------|---------|
| 口径非空 | 5 | caliber_desc 非空 | 空 → 扣 5 分（且该维度归零） |
| 业务定义 | 8 | 口径包含业务含义描述 | "订单金额" → 太简单，缺业务定义 → 扣 4 分 |
| 计算公式 | 7 | calc_formula 非空且可执行 | 空 → 扣 7 分 |
| 统计周期 | 5 | 口径中明确统计周期 | 缺"按自然月统计" → 扣 5 分 |

**口径三要素定义：**
```
完整口径 = 业务定义 + 计算公式 + 统计周期

示例（满分口径）：
  caliber_desc: "自然月内已完成订单的销售总金额，不含退款金额"
  calc_formula: "SUM(order_amount) WHERE order_status = 'completed' AND refund_status != 'refunded'"
  frequency: "月"

  → 业务定义 ✅："已完成订单的销售总金额，不含退款金额"
  → 计算公式 ✅：有明确 SQL 逻辑
  → 统计周期 ✅："自然月" + frequency=月
```

**大模型辅助口径质量评审：**
```
输入：caliber_desc + calc_formula + frequency
输出：
  {
    "has_business_def": true/false,     // 是否包含业务定义
    "has_formula": true/false,           // 是否有计算公式
    "has_period": true/false,            // 是否明确统计周期
    "def_quality": "clear/vague/missing", // 定义质量
    "issues": ["缺少退款处理说明", "未明确是否含税"],
    "score_adjustment": -4               // 建议扣分
  }
```

### 2.5 维度四：同名同义（20分）— 核心原则

| 检查项 | 分值 | 规则 | 扣分原因 |
|--------|------|------|---------|
| 无同名异义 | 10 | 全库无相同 metric_en + 不同 caliber_desc | 发现同名异义 → 扣 10 分（且该维度归零） |
| 无同义异名 | 7 | 全库无相同口径语义 + 不同 metric_en | 发现同义异名 → 扣 3 分/例 |
| 名称口径一致 | 3 | metric_cn 与 caliber_desc 语义一致 | "订单金额"但口径是"退款金额" → 扣 3 分 |

**检查逻辑：**
```
同名异义检查（硬性，一票否决该维度）：
  1. 全库扫描，按 metric_en 分组
  2. 同一 metric_en 的 caliber_desc 是否完全一致（或语义一致）
  3. 不一致 → 同名异义 → 该维度 0 分

同义异名检查（大模型辅助）：
  1. 大模型对全库 caliber_desc 做语义聚类
  2. 同一语义簇内 metric_en 是否唯一
  3. 不唯一 → 同义异名 → 每例扣 3 分

名称口径一致性：
  1. 大模型判断 metric_cn 与 caliber_desc 是否语义一致
  2. 不一致 → 扣 3 分
```

### 2.6 维度五：血缘可查（10分）

| 检查项 | 分值 | 规则 | 扣分原因 |
|--------|------|------|---------|
| 血缘ID非空 | 3 | lineage_id 非空 | 空 → 扣 3 分 |
| 血缘记录存在 | 4 | lineage_id 在 lineage/*.json 中能找到记录 | 找不到 → 扣 4 分 |
| 字段级映射 | 3 | 血缘记录中包含 column_mappings | 无字段映射 → 扣 3 分 |

**检查逻辑：**
```python
def check_lineage(metric, lineage_records):
    score = 0
    reasons = []
    
    # 1. lineage_id 非空
    if not metric.get('lineage_id'):
        reasons.append("血缘ID为空")
    else:
        score += 3
        
        # 2. 血缘记录存在
        lineage = lineage_records.get(metric['lineage_id'])
        if not lineage:
            reasons.append(f"血缘记录 {metric['lineage_id']} 不存在")
        else:
            score += 4
            
            # 3. 字段级映射
            if lineage.get('column_mappings'):
                score += 3
            else:
                reasons.append("血缘记录缺少字段级映射")
    
    return score, reasons
```

### 2.7 维度六：模型评审（10分）

| 检查项 | 分值 | 规则 | 扣分原因 |
|--------|------|------|---------|
| 经过模型评审 | 5 | source_model = "model_consensus" 或有评审记录 | source=manual → 扣 5 分 |
| 评审记录完整 | 3 | 评审 JSON 包含多模型结果 + 比对结论 | 评审记录缺失 → 扣 3 分 |
| 评审通过 | 2 | review_status = "approved" | pending/rejected → 扣 2 分 |

---

## 三、评分等级

| 等级 | 分数范围 | 含义 | 可执行操作 |
|------|---------|------|-----------|
| S | ≥ 95 | 优秀 | 可上线，可推广 |
| A | 85-94 | 合格 | 可上线 |
| B | 70-84 | 基本合格 | 需整改后复核 |
| C | 50-69 | 不合格 | 需重大整改 |
| D | < 50 | 严重不合格 | 禁止使用，必须重做 |

**特殊规则：**
- 同名异义 → 不管总分多少，最高只能评 C（一票否决升级）
- 口径为空 → 不管总分多少，最高只能评 C
- 拼音残留 → 不管总分多少，最高只能评 B

---

## 四、评分计算流程

### 4.1 自动评分流程

```
输入：单个指标记录 + 全库上下文（roots、lineage、reviews）

Step 1：命名规范检查
  ├── 拼音检测（pypinyin）
  ├── 英文名格式校验（正则）
  ├── 缩写拆解匹配（root_abbr 集合）
  └── → 得分 + 扣分原因

Step 2：词根关联检查
  ├── root_ids 非空检查
  ├── root_id 存在性检查（查 roots CSV）
  └── 英文名逆推检查（root_en 组合匹配）
  └── → 得分 + 扣分原因

Step 3：口径完整检查
  ├── caliber_desc 非空
  ├── calc_formula 非空
  ├── 大模型评审口径质量（三要素）
  └── → 得分 + 扣分原因

Step 4：同名同义检查
  ├── 全库 metric_en 分组查重
  ├── 大模型语义聚类查同义异名
  └── 大模型检查名称口径一致性
  └── → 得分 + 扣分原因

Step 5：血缘可查检查
  ├── lineage_id 非空
  ├── 查 lineage JSON 记录
  └── 检查 column_mappings
  └── → 得分 + 扣分原因

Step 6：模型评审检查
  ├── 检查 source_model
  ├── 查 reviews/ 评审记录
  └── 检查 review_status
  └── → 得分 + 扣分原因

Step 7：汇总
  ├── 六维度得分加总
  ├── 应用特殊规则（一票否决升级）
  ├── 确定等级
  └── 生成评分明细 + 待整改项
```

### 4.2 评分触发时机

| 时机 | 触发方式 | 说明 |
|------|---------|------|
| 指标创建后 | 自动触发 | 新建指标保存后立即评分 |
| 指标修改后 | 自动触发 | 任何字段变更后重新评分 |
| 词根库更新后 | 批量重评 | 词根变更可能影响关联指标评分 |
| 血缘文件更新后 | 批量重评 | 血缘变更影响关联指标评分 |
| 手动触发 | 按钮触发 | 用户点击"重新评分" |
| 定时全量 | 每日一次 | 夜间全量重评，保证评分时效性 |

---

## 五、评分数据存储

### 5.1 指标 CSV 新增字段

在指标 CSV 中新增 3 个评分字段：

```csv
metric_id,...,quality_score,quality_grade,quality_detail_file
M_SALE_002,...,85,A,scores/M_SALE_002.json
M_SALE_001,...,95,S,scores/M_SALE_001.json
M_SALE_010,...,72,B,scores/M_SALE_010.json
```

| 新增字段 | 说明 | 示例 |
|---------|------|------|
| quality_score | 质量评分（0-100） | 85 |
| quality_grade | 质量等级（S/A/B/C/D） | A |
| quality_detail_file | 评分明细文件路径 | scores/M_SALE_002.json |

### 5.2 评分明细 JSON 格式

文件路径：`scores/{metric_id}.json`

```json
{
  "metric_id": "M_SALE_002",
  "metric_cn": "本月订单金额",
  "metric_en": "monthly_order_amount",
  "total_score": 85,
  "grade": "A",
  "scored_at": "2026-08-03T10:30:00",
  "scored_by": "auto_scorer_v1",
  "dimensions": [
    {
      "dim_code": "naming",
      "dim_name": "命名规范",
      "score": 20,
      "max_score": 20,
      "status": "pass",
      "items": [
        {
          "item": "英文名标准化",
          "score": 8,
          "max": 8,
          "status": "pass",
          "reason": null
        },
        {
          "item": "缩写可拆解",
          "score": 6,
          "max": 6,
          "status": "pass",
          "reason": "mtd+ord+amt 全部匹配 root_abbr"
        },
        {
          "item": "无拼音残留",
          "score": 4,
          "max": 4,
          "status": "pass",
          "reason": null
        },
        {
          "item": "命名格式一致",
          "score": 2,
          "max": 2,
          "status": "pass",
          "reason": null
        }
      ]
    },
    {
      "dim_code": "root_link",
      "dim_name": "词根关联",
      "score": 15,
      "max_score": 15,
      "status": "pass",
      "items": [
        {
          "item": "词根非空",
          "score": 5,
          "max": 5,
          "status": "pass",
          "reason": null
        },
        {
          "item": "词根存在",
          "score": 5,
          "max": 5,
          "status": "pass",
          "reason": "R_SALE_001, R_SALE_002 均存在"
        },
        {
          "item": "词根可逆推",
          "score": 5,
          "max": 5,
          "status": "pass",
          "reason": "monthly(order+amount) 可逆推"
        }
      ]
    },
    {
      "dim_code": "caliber",
      "dim_name": "口径完整",
      "score": 20,
      "max_score": 25,
      "status": "warn",
      "items": [
        {
          "item": "口径非空",
          "score": 5,
          "max": 5,
          "status": "pass",
          "reason": null
        },
        {
          "item": "业务定义",
          "score": 8,
          "max": 8,
          "status": "pass",
          "reason": "自然月内已完成订单的销售总金额，不含退款金额"
        },
        {
          "item": "计算公式",
          "score": 7,
          "max": 7,
          "status": "pass",
          "reason": "SUM(order_amount) WHERE order_status='completed'"
        },
        {
          "item": "统计周期",
          "score": 0,
          "max": 5,
          "status": "fail",
          "reason": "口径中未明确统计周期，frequency=月 但口径描述中缺少"自然月"说明"
        }
      ]
    },
    {
      "dim_code": "same_name",
      "dim_name": "同名同义",
      "score": 20,
      "max_score": 20,
      "status": "pass",
      "items": [
        {
          "item": "无同名异义",
          "score": 10,
          "max": 10,
          "status": "pass",
          "reason": "全库无同名 metric_en 冲突"
        },
        {
          "item": "无同义异名",
          "score": 7,
          "max": 7,
          "status": "pass",
          "reason": null
        },
        {
          "item": "名称口径一致",
          "score": 3,
          "max": 3,
          "status": "pass",
          "reason": null
        }
      ]
    },
    {
      "dim_code": "lineage",
      "dim_name": "血缘可查",
      "score": 10,
      "max_score": 10,
      "status": "pass",
      "items": [
        {
          "item": "血缘ID非空",
          "score": 3,
          "max": 3,
          "status": "pass",
          "reason": "LN_SALE_001"
        },
        {
          "item": "血缘记录存在",
          "score": 4,
          "max": 4,
          "status": "pass",
          "reason": null
        },
        {
          "item": "字段级映射",
          "score": 3,
          "max": 3,
          "status": "pass",
          "reason": "column_mappings 包含 5 个字段映射"
        }
      ]
    },
    {
      "dim_code": "model_review",
      "dim_name": "模型评审",
      "score": 0,
      "max_score": 10,
      "status": "fail",
      "items": [
        {
          "item": "经过模型评审",
          "score": 0,
          "max": 5,
          "status": "fail",
          "reason": "source_model=manual，未经多模型评审"
        },
        {
          "item": "评审记录完整",
          "score": 0,
          "max": 3,
          "status": "fail",
          "reason": "reviews/ 目录下无评审记录"
        },
        {
          "item": "评审通过",
          "score": 0,
          "max": 2,
          "status": "fail",
          "reason": "review_status=pending"
        }
      ]
    }
  ],
  "special_rules": [],
  "issues": [
    {
      "priority": "P1",
      "dimension": "caliber",
      "issue": "口径缺少统计周期描述",
      "suggestion": "在 caliber_desc 中补充"按自然月统计"",
      "fix_action": "修改 caliber_desc"
    },
    {
      "priority": "P1",
      "dimension": "model_review",
      "issue": "未经大模型评审",
      "suggestion": "提交多模型评审流程",
      "fix_action": "点击"提交评审"按钮"
    }
  ],
  "score_history": [
    {
      "scored_at": "2026-08-02T22:00:00",
      "score": 80,
      "grade": "B",
      "trigger": "scheduled"
    },
    {
      "scored_at": "2026-08-03T10:30:00",
      "score": 85,
      "grade": "A",
      "trigger": "metric_updated"
    }
  ]
}
```

### 5.3 评分汇总文件

文件路径：`scores/_summary.csv`

```csv
metric_id,metric_cn,quality_score,quality_grade,naming,root_link,caliber,same_name,lineage,model_review,issues_count,last_scored_at
M_SALE_001,订单金额,95,S,20,15,25,20,10,5,1,2026-08-03T10:30
M_SALE_002,本月订单金额,85,A,20,15,20,20,10,0,2,2026-08-03T10:30
M_SALE_010,客单价,72,B,20,15,15,20,0,2,3,2026-08-03T10:30
M_MALL_005,出租率,58,C,0,15,25,20,0,0,4,2026-08-03T10:30
```

---

## 六、指标详情页评分展示

### 6.1 详情页布局

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 返回列表                                                      │
│                                                                 │
│  M_SALE_002  本月订单金额  monthly_order_amount                   │
│  主题域：sale  |  类型：derived  |  负责人：张三  |  S级          │
│                                                                 │
│  ═══════════════════════════════════════════════════════════════│
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    质量评分                                   ││
│  │                                                              ││
│  │    ┌──────────┐   ┌──────────────────────────────────────┐  ││
│  │    │          │   │  命名规范   ████████████████████ 20/20│  ││
│  │    │   85     │   │  词根关联   ████████████████████ 15/15│  ││
│  │    │   ───    │   │  口径完整   ████████████████░░░░ 20/25│  ││
│  │    │    A     │   │  同名同义   ████████████████████ 20/20│  ││
│  │    │  合格 ✅  │   │  血缘可查   ████████████████████ 10/10│  ││
│  │    │          │   │  模型评审   ░░░░░░░░░░░░░░░░░░░░  0/10│  ││
│  │    └──────────┘   └──────────────────────────────────────┘  ││
│  │                                                              ││
│  │    评分时间：2026-08-03 10:30  |  触发：指标修改后自动评分    ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ═══════════════════════════════════════════════════════════════│
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    评分明细                                   ││
│  │                                                              ││
│  │  ▸ 命名规范  20/20  ✅                                       ││
│  │    ├─ 英文名标准化        8/8   ✅                           ││
│  │    ├─ 缩写可拆解          6/6   ✅  mtd+ord+amt 全匹配       ││
│  │    ├─ 无拼音残留          4/4   ✅                           ││
│  │    └─ 命名格式一致        2/2   ✅                           ││
│  │                                                              ││
│  │  ▸ 词根关联  15/15  ✅                                       ││
│  │    ├─ 词根非空            5/5   ✅                           ││
│  │    ├─ 词根存在            5/5   ✅  R_SALE_001, R_SALE_002   ││
│  │    └─ 词根可逆推          5/5   ✅  order+amount 可逆推      ││
│  │                                                              ││
│  │  ▸ 口径完整  20/25  ⚠️                                       ││
│  │    ├─ 口径非空            5/5   ✅                           ││
│  │    ├─ 业务定义            8/8   ✅                           ││
│  │    ├─ 计算公式            7/7   ✅                           ││
│  │    └─ 统计周期            0/5   ❌  口径中未明确"自然月"      ││
│  │                                                              ││
│  │  ▸ 同名同义  20/20  ✅                                       ││
│  │    ├─ 无同名异义         10/10  ✅                           ││
│  │    ├─ 无同义异名          7/7   ✅                           ││
│  │    └─ 名称口径一致        3/3   ✅                           ││
│  │                                                              ││
│  │  ▸ 血缘可查  10/10  ✅                                       ││
│  │    ├─ 血缘ID非空          3/3   ✅  LN_SALE_001              ││
│  │    ├─ 血缘记录存在        4/4   ✅                           ││
│  │    └─ 字段级映射          3/3   ✅  5个字段映射               ││
│  │                                                              ││
│  │  ▸ 模型评审   0/10  ❌                                       ││
│  │    ├─ 经过模型评审        0/5   ❌  source=manual             ││
│  │    ├─ 评审记录完整        0/3   ❌  无评审记录                ││
│  │    └─ 评审通过            0/2   ❌  review_status=pending    ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ═══════════════════════════════════════════════════════════════│
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    待整改项（2项）                             ││
│  │                                                              ││
│  │  P1  [口径] 口径缺少统计周期描述                              ││
│  │      → 在 caliber_desc 中补充"按自然月统计"                   ││
│  │      [修改口径]                                              ││
│  │                                                              ││
│  │  P1  [评审] 未经大模型评审                                    ││
│  │      → 提交多模型评审流程                                     ││
│  │      [提交评审]                                              ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ═══════════════════════════════════════════════════════════════│
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    评分历史                                   ││
│  │                                                              ││
│  │  时间              得分  等级  触发原因                       ││
│  │  ──────────────── ──── ──── ──────────────                  ││
│  │  08-03 10:30       85   A    指标修改后自动评分               ││
│  │  08-02 22:00       80   B    定时全量评分                     ││
│  │  08-01 14:20       75   B    指标创建后自动评分               ││
│  │                                                              ││
│  │  趋势：75 → 80 → 85  📈                                      ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 列表页评分展示

在指标列表中，每行显示评分概要：

```
┌───────────────────────────────────────────────────────────────────────┐
│  指标列表                                              [批量评分] [筛选]│
├───────────────┬────────────┬────────┬──────┬──────┬──────┬───────────┤
│  指标ID        │ 中文名     │ 英文名  │ 等级 │ 评分 │ 状态 │ 待整改    │
├───────────────┼────────────┼────────┼──────┼──────┼──────┼───────────┤
│  M_SALE_001    │ 订单金额   │ ord_amt│  S   │  95  │ ✅   │ 1项       │
│  M_SALE_002    │ 本月订单.. │ mtd_.. │  A   │  85  │ ✅   │ 2项       │
│  M_SALE_010    │ 客单价     │ avg_.. │  B   │  72  │ ⚠️   │ 3项       │
│  M_MALL_005    │ 出租率     │ occ_.. │  C   │  58  │ ❌   │ 4项       │
│  M_SALE_003    │ 订单数     │ ord_.. │  A   │  88  │ ✅   │ 1项       │
└───────────────┴────────────┴────────┴──────┴──────┴──────┴───────────┘

支持按评分排序、按等级筛选、按待整改项筛选
```

### 6.3 看板联动

在治理总览看板中新增"指标评分分布"模块：

```
┌─────────────────────────────────────────────────────┐
│  指标质量评分分布                                     │
│                                                      │
│  S级(≥95)  ████              280  (15%)             │
│  A级(85-94) ████████████     650  (35%)             │
│  B级(70-84) ██████████       560  (30%)  ⚠️         │
│  C级(50-69) ████             280  (15%)  ❌         │
│  D级(<50)   █                76   (5%)   ❌         │
│                                                      │
│  平均分：82.3    中位数：85    待整改指标：836个      │
│                                                      │
│  [查看低分指标清单]  [批量重新评分]                    │
└─────────────────────────────────────────────────────┘
```

---

## 七、评分规则配置

### 7.1 规则配置文件

文件路径：`config/scoring_rules.json`

```json
{
  "version": "1.0",
  "total_max_score": 100,
  "grade_thresholds": {
    "S": 95,
    "A": 85,
    "B": 70,
    "C": 50,
    "D": 0
  },
  "dimensions": [
    {
      "dim_code": "naming",
      "dim_name": "命名规范",
      "max_score": 20,
      "items": [
        {"item": "英文名标准化", "max": 8},
        {"item": "缩写可拆解", "max": 6},
        {"item": "无拼音残留", "max": 4, "veto": true},
        {"item": "命名格式一致", "max": 2}
      ]
    },
    {
      "dim_code": "root_link",
      "dim_name": "词根关联",
      "max_score": 15,
      "items": [
        {"item": "词根非空", "max": 5},
        {"item": "词根存在", "max": 5},
        {"item": "词根可逆推", "max": 5}
      ]
    },
    {
      "dim_code": "caliber",
      "dim_name": "口径完整",
      "max_score": 25,
      "items": [
        {"item": "口径非空", "max": 5, "veto": true},
        {"item": "业务定义", "max": 8},
        {"item": "计算公式", "max": 7},
        {"item": "统计周期", "max": 5}
      ]
    },
    {
      "dim_code": "same_name",
      "dim_name": "同名同义",
      "max_score": 20,
      "items": [
        {"item": "无同名异义", "max": 10, "veto": true},
        {"item": "无同义异名", "max": 7, "deduct_per_case": 3},
        {"item": "名称口径一致", "max": 3}
      ]
    },
    {
      "dim_code": "lineage",
      "dim_name": "血缘可查",
      "max_score": 10,
      "items": [
        {"item": "血缘ID非空", "max": 3},
        {"item": "血缘记录存在", "max": 4},
        {"item": "字段级映射", "max": 3}
      ]
    },
    {
      "dim_code": "model_review",
      "dim_name": "模型评审",
      "max_score": 10,
      "items": [
        {"item": "经过模型评审", "max": 5},
        {"item": "评审记录完整", "max": 3},
        {"item": "评审通过", "max": 2}
      ]
    }
  ],
  "special_rules": [
    {
      "rule": "同名异义",
      "effect": "max_grade_C",
      "description": "存在同名异义时，无论总分多少，最高只能评 C 级"
    },
    {
      "rule": "口径为空",
      "effect": "max_grade_C",
      "description": "口径描述为空时，最高只能评 C 级"
    },
    {
      "rule": "拼音残留",
      "effect": "max_grade_B",
      "description": "英文名/缩写中检测到拼音残留时，最高只能评 B 级"
    }
  ]
}
```

### 7.2 规则可配置性

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 各维度分值权重 | 可调整每个维度的满分值 | 20/15/25/20/10/10 |
| 等级阈值 | 可调整 S/A/B/C/D 的分数线 | 95/85/70/50/0 |
| 一票否决项 | 可启用/禁用特定否决规则 | 全部启用 |
| 大模型评审维度 | 口径质量评审是否启用大模型 | 启用 |
| 扣分粒度 | 同义异名每例扣分值 | 3 分/例 |

---

## 八、与项目级验收评分的关系

### 8.1 两套评分的定位

```
指标级评分（本文档）          项目级验收评分（验收评分标准.md）
       │                              │
       ▼                              ▼
  单个指标的质量                  整个项目的治理成熟度
       │                              │
       ▼                              ▼
  决定指标能否上线              决定项目是否验收通过
       │                              │
       └──────────┬───────────────────┘
                  ▼
          项目级评分 = 所有指标级评分的聚合
```

### 8.2 聚合关系

```
项目验收评分 = f(所有指标的 quality_score)

具体聚合方式：
  指标平均分 = AVG(quality_score)                    → 占项目分 40%
  指标达标率 = COUNT(quality_score >= 85) / COUNT(*)  → 占项目分 30%
  零缺陷率  = COUNT(issues_count = 0) / COUNT(*)     → 占项目分 15%
  整改完成率 = COUNT(已整改) / COUNT(待整改)          → 占项目分 15%
```

---

## 九、实现方案

### 9.1 评分引擎架构

```
┌─────────────────────────────────────────────────────────┐
│                    评分引擎 (scoring_engine)              │
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ 规则加载器 │  │ 数据加载器│  │ 评分执行器│  │ 结果输出 │ │
│  │          │  │          │  │          │  │         │ │
│  │ 读取     │  │ 读取     │  │ 逐维度   │  │ 写入    │ │
│  │ scoring  │  │ metrics  │  │ 执行评分 │  │ scores/ │ │
│  │ _rules   │  │ roots    │  │          │  │ JSON    │ │
│  │ .json    │  │ lineage  │  │          │  │         │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │              大模型评审接口                       │   │
│  │  ├── 口径质量评审（三要素检测）                   │   │
│  │  ├── 同义异名语义聚类                             │   │
│  │  └── 名称口径一致性判断                           │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 9.2 评分引擎核心逻辑（伪代码）

```python
class MetricScorer:
    def __init__(self, rules, roots_data, lineage_data, reviews_data):
        self.rules = rules
        self.roots = roots_data
        self.lineage = lineage_data
        self.reviews = reviews_data
    
    def score_metric(self, metric) -> ScoreResult:
        """对单个指标评分"""
        result = ScoreResult(metric_id=metric['metric_id'])
        
        # 逐维度评分
        for dim_rule in self.rules['dimensions']:
            dim_score = self._score_dimension(metric, dim_rule)
            result.add_dimension(dim_score)
        
        # 汇总
        result.calculate_total()
        
        # 应用特殊规则
        result.apply_special_rules(self.rules['special_rules'])
        
        # 确定等级
        result.determine_grade(self.rules['grade_thresholds'])
        
        # 生成待整改项
        result.generate_issues()
        
        return result
    
    def _score_dimension(self, metric, dim_rule):
        """评分单个维度"""
        dim = DimensionScore(dim_code=dim_rule['dim_code'])
        
        for item_rule in dim_rule['items']:
            item_score = self._check_item(metric, item_rule)
            dim.add_item(item_score)
        
        dim.calculate_subtotal()
        return dim
    
    def _check_item(self, metric, item_rule) -> ItemScore:
        """检查单个评分项"""
        item = ItemScore(item=item_rule['item'], max_score=item_rule['max'])
        
        if item_rule['item'] == '英文名标准化':
            ok = self._check_en_standardized(metric['metric_en'])
            item.set_score(item_rule['max'] if ok else 0, 
                          None if ok else '英文名不符合标准格式')
        
        elif item_rule['item'] == '无拼音残留':
            has_pinyin, pinyin_token = self._check_pinyin(metric['metric_en'])
            if not has_pinyin:
                item.set_score(item_rule['max'], None)
            else:
                item.set_score(0, f'检测到拼音片段: {pinyin_token}')
                if item_rule.get('veto'):
                    item.set_veto(True)
        
        # ... 其他检查项
        
        return item
```

### 9.3 文件结构

```
data-governance/
├── config/
│   ├── scoring_rules.json      ← 评分规则配置
│   ├── modifier_rules.csv
│   └── metric_tree.csv
├── roots/
│   ├── sale_roots.csv
│   └── ...
├── metrics/
│   ├── sale_metrics.csv        ← 新增 quality_score, quality_grade, quality_detail_file 字段
│   └── ...
├── lineage/
│   └── *.json
├── reviews/
│   └── *.json
└── scores/                     ← 新增目录
    ├── _summary.csv            ← 评分汇总
    ├── M_SALE_001.json         ← 单指标评分明细
    ├── M_SALE_002.json
    └── ...
```

---

## 十、总结

| 要点 | 说明 |
|------|------|
| 评分对象 | 每个指标独立评分，0-100 分 |
| 评分维度 | 6 大维度：命名规范、词根关联、口径完整、同名同义、血缘可查、模型评审 |
| 评分等级 | S(≥95) / A(85-94) / B(70-84) / C(50-69) / D(<50) |
| 一票否决 | 同名异义、口径为空 → 最高 C 级；拼音残留 → 最高 B 级 |
| 展示方式 | 指标详情页显示总分、等级、六维度明细、扣分原因、待整改项、评分历史 |
| 存储方式 | 指标 CSV 存概要分数 + scores/ 目录存明细 JSON |
| 触发时机 | 创建后、修改后、词根/血缘更新后、定时全量 |
| 规则可配 | 分值权重、等级阈值、否决项均可通过 JSON 配置调整 |
| 与验收评分关系 | 指标级评分是项目级验收评分的基础数据源 |
