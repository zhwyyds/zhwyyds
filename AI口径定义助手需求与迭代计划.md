# AI 口径定义助手 — 需求规格与迭代计划

> 编制：2026-08-05 ｜ 编制视角：高级项目经理
> 需求来源：用户明确「后续指标的指标定义可能比较含糊，需依赖 AI 能力进行评估并精准定义，不能依赖人员填写，人只做核查」
> 配套：功能缺失审查报告.md、项目优化建议报告.md

---

## 一、需求目标

把「口径定义」从**人工填写**改为**AI 起草 + 人工核查**：

```
含糊指标定义（名称 + 零散描述）
   │
   ▼
[1] 多模型起草（3 模型各自产出结构化口径 + 含糊点评估）
   │
   ▼
[2] 共识汇总（字段级比对 → 推荐版 + 差异说明）
   │
   ▼
[3] 落库待核查（caliber_status = pending）
   │
   ▼
[4] 人工核查（批准 / 修改 / 打回，打回附原因可重新起草）
   │
   ▼
[5] 批准后写入正式字段 + 触发重新评分
   │
   ▼
[6] 评分闭环：口径完整维度提升 → 等级变化可见
```

**人的角色从「填写者」降级为「核查者」**，全流程留痕。

---

## 二、结构化口径字段（caliber_*，扩展 metrics CSV）

| 字段 | 说明 | 示例（月度销售额） |
|---|---|---|
| caliber_business | 业务定义（是什么） | 自然月内已完成订单的销售总金额，不含退款 |
| caliber_formula | 计算公式 | SUM(sale_amt) WHERE order_status='completed' AND stat_month=当前自然月 |
| caliber_period | 统计周期 | 月度 |
| caliber_granularity | 计算粒度 | 订单行级 |
| caliber_boundary | 边界/排除条件 | 不含退款、不含未支付、按支付时间归属 |
| caliber_source | 来源表/字段 | dwd_sale_order_df.sale_amt |
| caliber_owner | 口径责任人 | 交易分析部（人核查时确认） |
| caliber_status | 核查状态 | pending / approved / edited / rejected |
| caliber_ai_by | 起草模型+共识 | gpt-4o;claude-3.5-sonnet;qwen-plus(consensus) |
| caliber_checked_by | 核查人 | 操作人 |
| caliber_checked_at | 核查时间 | 2026-08-05 11:30 |

> 旧字段 `caliber_desc` / `tech_caliber` 保留为汇总视图（由结构化字段拼接生成），避免破坏现有评分/导出逻辑。

---

## 三、核查状态机

```
                 ┌── 批准 ──→ approved ──→ 写正式字段 + 触发重评分
AI 起草 ──→ pending ──┤
                 ├── 修改 ──→ edited ──→（人改后视为 approved）
                 └── 打回 ──→ rejected ──→ 附原因 → 可选重新起草 → pending
```

- 状态迁移写审计：谁核查、何时、改了什么字段。
- 发布门禁联动：`caliber_status != approved` 的指标**禁止发布**（与发布质量门禁合并实现）。

---

## 四、多模型共识策略

- 复用现有 `llm/parallel.py` + `llm/factory.py`（mock / live 双模，Mock 用于开发与 CI）。
- 3 模型（gpt-4o / claude-3.5-sonnet / qwen-plus）各自出稿 → 新增「口径共识比对」：逐字段取多数/最优，产出推荐版 + **字段级差异说明**。
- 一致性规则：业务定义 3 模型一致 < 2/3 时标记「高风险」，核查列表置顶提示。

---

## 五、触发时机

| 时机 | 说明 |
|---|---|
| 批量生成联动 | 迭代 2 批量生成指标时，自动进入口径起草 |
| 存量一键补全 | 对已有「口径为空/含糊」指标批量补口径 |
| 单指标重起草 | 打回后重试 / 手动触发 |

---

## 六、与现有模块联动

| 模块 | 联动方式 |
|---|---|
| scoring「口径完整」维度 | 检查项升级：结构化字段非空率 + 公式可执行性 + 边界完整性（替代现在的文本粗判） |
| 发布门禁（P1） | 口径未 approved 禁发 |
| 验收引擎「口径覆盖/质量」 | 直接受益：覆盖率从 caliber_business 非空统计 |
| 批量生成（P0-1） | 生成即起草，形成「生成 → 起草 → 核查 → 评分 → 发布」全自动链路 |

---

## 七、验收标准（口径助手）

1. 输入含糊定义 → 产出 9 个结构化字段 + 含糊点/风险清单；
2. 核查状态机闭环走通：批准 / 修改 / 打回（含打回重起草）；
3. 批准后 caliber_* 落 CSV、触发重评分、等级变化前端可见；
4. 多模型字段级差异说明可展示；
5. 发布门禁拦截未批准口径的指标。

---

## 八、迭代计划（更新版）

### 迭代 1（本周）— 质量与运维底线
| # | 任务 | 验收 |
|---|---|---|
| 1 | Task #17 收尾：scoring/release/CLI 测试 | pytest 全绿、ruff/mypy 过 |
| 2 | `data-governance validate` 数据自检命令 | CSV 字段数/必填/编码/引用一键体检 |
| 3 | 每日备份脚本 | 快照 backup/ + 保留 N 天 + 恢复步骤 |
| 4 | GitHub Actions CI | push/PR 自动 ruff+mypy+pytest+coverage |
| 5 | 运维手册 | 启动/排障/日志/恢复 |
| 6 | 口径字段标准 + CSV 迁移方案定稿 | 结构化字段落库设计评审通过 |

### 迭代 2 — 核心产能（P0）
| # | 任务 |
|---|---|
| 1 | **批量生成后端**：原子×修饰词组合 + 校验 + 批量入库（接真接口） |
| 2 | **词根写 API**：POST/PUT /api/roots |
| 3 | **血缘上传 API**：POST /api/lineage/upload（JSON 校验落盘） |
| 4 | **口径助手-起草**：prompt + parsing + 多模型出稿 + 共识比对 |
| 5 | **口径助手-核查流**：状态机 + 核查 API（待核查队列/批准/修改/打回）+ 前端核查中心页 |
| 6 | **口径助手-联动**：批量生成联动 + 存量补全 + 评分/发布联动 |
| 7 | 前端：批量生成真接口 + 词根维护 UI + 口径核查 UI |

### 迭代 3 — 治理闭环
发布门禁/撤销/版本对比、域级治理看板（红绿灯）、黄金演示数据（S/A/B/C + 反例）、文档体系（索引/CHANGELOG/操作手册）。

---
*本规格为 PM 评审版；迭代 2 开工前需开发侧确认字段迁移与 API 契约。*
