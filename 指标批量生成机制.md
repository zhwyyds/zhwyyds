# 指标批量生成机制 — 原子指标 + 修饰词派生方案

> 版本：1.0
> 日期：2026-08-03
> 解决问题：2000个指标不该一个一个手动建，通过"原子指标 × 修饰词"自动派生

---

## 一、核心思路

老大举的例子很典型：

```
订单金额（原子指标）
  ├── 本月订单金额
  ├── 本年订单金额
  ├── 累计订单金额
  ├── 年累计订单金额
  ├── 实付订单金额 ← 这是另一个原子指标
  │     ├── 本月实付订单金额
  │     ├── 本年实付订单金额
  │     └── ...
  └── 订单金额环比增长率
```

本质上：**大部分指标 = 原子指标 + 修饰词组合**

所以方案是：
1. 只手动定义**原子指标**（预计 100-150 个）
2. 维护一套**修饰词库**（时间/对比/维度，预计 20-30 个）
3. **批量派生**或**按需生成**衍生指标
4. 大模型辅助推荐常用组合 + 自动生成口径描述

**效果预估：** 150 个原子 × 15 个常用修饰词 = 理论上 2250 个衍生指标，实际按需生成覆盖 2000 指标绰绰有余。

---

## 二、指标三层模型

```
┌─────────────────────────────────────────────────────┐
│  L3 复合指标（Composite）                            │
│  多个指标计算得出，如：客单价 = 订单金额 / 订单数    │
│  数量预估：50-100 个                                 │
├─────────────────────────────────────────────────────┤
│  L2 派生指标（Derived）                              │
│  原子指标 + 修饰词，如：本月订单金额                 │
│  数量预估：1500-1800 个                              │
│  ★ 批量生成 / 按需生成，不需手动逐个创建             │
├─────────────────────────────────────────────────────┤
│  L1 原子指标（Atomic）                               │
│  不可再分的基础业务指标，如：订单金额                 │
│  数量预估：100-150 个                                │
│  ★ 需要人工定义 + 大模型辅助生成                     │
└─────────────────────────────────────────────────────┘
```

---

## 三、修饰词库设计

### 3.1 时间修饰词

| 修饰词ID | 中文 | 英文 | 缩写 | 说明 | 口径模板 |
|----------|------|------|------|------|----------|
| T001 | 本月 | month_to_date | mtd | 当前自然月 | {当前自然月}内{原子口径} |
| T002 | 本年 | year_to_date | ytd | 当前自然年 | {当前自然年}内{原子口径} |
| T003 | 本季 | quarter_to_date | qtd | 当前自然季度 | {当前自然季度}内{原子口径} |
| T004 | 本周 | week_to_date | wtd | 当前自然周 | {当前自然周}内{原子口径} |
| T005 | 累计 | cumulative | cum | 从起始日期到当前 | 从{起始日期}到当前{原子口径} |
| T006 | 年累计 | yearly_cumulative | ycum | 当年1月至今累计 | 当年1月1日至今{原子口径} |
| T007 | 上月 | last_month | lm | 上一个自然月 | {上一个自然月}内{原子口径} |
| T008 | 上年同期 | last_year_same_period | lysp | 去年同一时期 | {上年同期}内{原子口径} |
| T009 | 日均 | daily_average | day_avg | 按天平均 | {统计周期}内每日{原子口径}的均值 |
| T010 | 月均 | monthly_average | mon_avg | 按月平均 | {统计周期}内每月{原子口径}的均值 |

### 3.2 对比修饰词

| 修饰词ID | 中文 | 英文 | 缩写 | 说明 | 口径模板 |
|----------|------|------|------|------|----------|
| C001 | 环比 | month_over_month | mom | 与上月对比 | {本月原子口径} vs {上月原子口径}的变化率 |
| C002 | 同比 | year_over_year | yoy | 与上年同期对比 | {本年原子口径} vs {上年同期原子口径}的变化率 |
| C003 | 环比差值 | mom_diff | mom_d | 与上月差值 | {本月原子口径} - {上月原子口径} |
| C004 | 同比差值 | yoy_diff | yoy_d | 与上年差值 | {本年原子口径} - {上年同期原子口径} |
| C005 | 达成率 | achievement_rate | ach_rte | 实际/目标 | {实际原子口径} / {目标原子口径} × 100% |

### 3.3 业务维度修饰词

| 修饰词ID | 中文 | 英文 | 缩写 | 说明 |
|----------|------|------|------|------|
| D001 | 按商场 | by_mall | by_mall | 按商场维度拆分 |
| D002 | 按商户 | by_partner | by_ptnr | 按商户维度拆分 |
| D003 | 按品类 | by_category | by_cat | 按商品品类拆分 |
| D004 | 按楼层 | by_floor | by_flr | 按楼层拆分 |
| D005 | 按区域 | by_region | by_reg | 按区域拆分 |
| D006 | 按渠道 | by_channel | by_chn | 按渠道拆分 |

### 3.4 修饰词库存储

文件路径：`config/modifiers.csv`

```csv
modifier_id,modifier_cn,modifier_en,modifier_abbr,modifier_type,description,caliber_template,applicable_metric_types
T001,本月,month_to_date,mtd,time,当前自然月,"{period}内{caliber}",sum,count,avg
T002,本年,year_to_date,ytd,time,当前自然年,"{period}内{caliber}",sum,count,avg
C001,环比,month_over_month,mom,compare,与上月对比变化率,"({current_period}{caliber} - {last_period}{caliber}) / {last_period}{caliber} × 100%",sum,count,avg
D001,按商场,by_mall,by_mall,business,按商场维度拆分,"{caliber}，按商场维度",sum,count,avg,max,min
```

---

## 四、批量生成机制

### 4.1 两种生成模式

#### 模式一：批量生成（Batch）

**适用场景：** 新原子指标建好后，一次性生成所有常用衍生指标。

```
操作流程：
1. 选定原子指标（可多选）
2. 勾选要应用的修饰词（可多选）
3. 系统自动组合生成所有衍生指标
4. 大模型批量生成口径描述
5. 大模型批量评审
6. 人工批量确认（不是逐个，是批量审核）
```

**示例：**
```
选定原子指标：订单金额、实付订单金额
勾选修饰词：[本月] [本年] [累计] [年累计] [环比] [同比]

生成结果：
├── mtd_ord_amt        本月订单金额
├── ytd_ord_amt        本年订单金额
├── cum_ord_amt        累计订单金额
├── ycum_ord_amt       年累计订单金额
├── mom_ord_amt        订单金额环比
├── yoy_ord_amt        订单金额同比
├── mtd_paid_ord_amt   本月实付订单金额
├── ytd_paid_ord_amt   本年实付订单金额
├── cum_paid_ord_amt   累计实付订单金额
├── ycum_paid_ord_amt  年累计实付订单金额
├── mom_paid_ord_amt   实付订单金额环比
└── yoy_paid_ord_amt   实付订单金额同比
→ 2个原子 × 6个修饰词 = 12个衍生指标，一键生成
```

#### 模式二：按需生成（On-demand）

**适用场景：** 临时需要某个特定维度的指标，不需要预生成所有组合。

```
操作流程：
1. 选定原子指标
2. 选择修饰词（可组合多个，如"本月"+"按商场"）
3. 系统即时生成
4. 大模型即时评审
5. 人工确认入库
```

**示例：**
```
选定：订单金额 + [本月] + [按商场]
生成：mtd_ord_amt_by_mall  本月各商场订单金额
→ 按需生成，不污染指标库
```

### 4.2 命名规则

衍生指标命名 = `{时间修饰缩写}_{原子指标缩写}_{对比修饰缩写}_{维度修饰缩写}`

```
规则：[时间修饰]_[原子指标]_[对比修饰]_[维度修饰]
     可选          必填        可选        可选

示例：
ord_amt                    → 订单金额（原子，无修饰）
mtd_ord_amt                → 本月订单金额（+时间）
ytd_ord_amt                → 本年订单金额（+时间）
mom_ord_amt                → 订单金额环比（+对比）
mtd_ord_amt_by_mall        → 本月各商场订单金额（+时间+维度）
mom_ord_amt_by_ptnr        → 各商户订单金额环比（+对比+维度）
```

### 4.3 口径自动生成

口径描述通过模板自动拼接：

```
原子口径 = "自然月内已完成订单的销售总金额，不含退款"

+ T001(本月) → "当前自然月内已完成订单的销售总金额，不含退款"
+ T002(本年) → "当前自然年内已完成订单的销售总金额，不含退款"
+ C001(环比) → "本月订单金额与上月订单金额的变化率：(本月订单金额-上月订单金额)/上月订单金额×100%"

大模型负责：
- 优化口径描述的自然语言表达
- 检查拼接后的口径是否有逻辑问题
- 补充遗漏的业务条件
```

---

## 五、大模型在批量生成中的角色

### 5.1 智能推荐

```
输入：原子指标 = "订单金额"
大模型推荐常用组合：
  ✅ 强烈推荐：[本月][本年][环比][同比] — 财务核心指标必配
  ✅ 推荐：[累计][年累计] — 经营分析常用
  ⬜ 可选：[本月+按商场][本年+按商户] — 管理报表可能用
  ❌ 不推荐：[日均] — 订单金额按天平均无业务意义
```

### 5.2 口径生成

```
输入：原子指标口径 + 修饰词
大模型生成：通顺的、无歧义的口径描述

原子口径："已完成订单的销售总金额，不含退款"
修饰词：本月

模型A输出："当前自然月内已完成订单的销售总金额，不含退款"
模型B输出："本月度内所有已完成订单的销售总金额（不含退款）"
模型C输出："当前自然月内，已完成订单的销售金额合计，不含退款金额"

比对 → 取共识 → 微调 → "当前自然月内已完成订单的销售总金额，不含退款"
```

### 5.3 合理性检查

```
大模型检查：
  ❌ "订单金额日均" — 订单金额是金额型，按天平均可能无业务意义 → 标记警告
  ❌ "出租率同比" — 出租率本身是比率，再算同比变化率可能造成歧义 → 建议用差值
  ✅ "本月订单金额" — 合理
  ✅ "各商场出租率" — 合理
```

---

## 六、衍生指标 CSV 格式

衍生指标在 metrics CSV 中增加 `parent_metric_id` 和 `modifier_ids` 字段：

```csv
metric_id,metric_cn,metric_en,metric_abbr,domain_code,metric_type,parent_metric_id,modifier_ids,root_ids,caliber_desc,unit,frequency,owner,source_model,review_status,created_at,updated_at
M_SALE_001,订单金额,order_amount,ord_amt,sale,atomic,,,"R_SALE_001;R_SALE_002",已完成订单的销售总金额不含退款,元,月,,model_consensus,approved,2026-08-03,2026-08-03
M_SALE_002,本月订单金额,monthly_order_amount,mtd_ord_amt,sale,derived,M_SALE_001,"T001","R_SALE_001;R_SALE_002",当前自然月内已完成订单的销售总金额不含退款,元,月,,batch_generated,approved,2026-08-03,2026-08-03
M_SALE_003,本年订单金额,yearly_order_amount,ytd_ord_amt,sale,derived,M_SALE_001,"T002","R_SALE_001;R_SALE_002",当前自然年内已完成订单的销售总金额不含退款,元,年,,batch_generated,approved,2026-08-03,2026-08-03
M_SALE_004,订单金额环比,order_amount_mom,mom_ord_amt,sale,derived,M_SALE_001,"C001","R_SALE_001;R_SALE_002",本月订单金额与上月订单金额的变化率,%,月,,batch_generated,pending,2026-08-03,2026-08-03
M_SALE_005,本月各商场订单金额,monthly_order_amount_by_mall,mtd_ord_amt_by_mall,sale,derived,M_SALE_001,"T001;D001","R_SALE_001;R_SALE_002;R_MALL_001",当前自然月内各商场已完成订单的销售总金额不含退款,元,月,,batch_generated,pending,2026-08-03,2026-08-03
```

**新增字段说明：**
- `parent_metric_id`：父指标ID（原子指标的ID），衍生指标必填
- `modifier_ids`：应用的修饰词ID，多个用分号分隔
- `source_model`：新增 `batch_generated` 值，表示批量生成

---

## 七、工作量对比

| 方式 | 手动工作量 | 效果 |
|------|-----------|------|
| **旧方案：逐个手动** | 2000 个指标 × (命名+口径+词根关联+评审) ≈ 2000 × 5分钟 = 167 小时 | 😱 |
| **新方案：原子+派生** | 150 原子 × 5分钟 + 修饰词库 30 个 × 3分钟 + 批量生成+批量确认 ≈ 20 小时 | ✅ |
| **效率提升** | **约 8 倍** | 🚀 |

---

## 八、实施建议

### 8.1 先做什么

1. **先建修饰词库**（config/modifiers.csv）— 1天
2. **从 2 个主题域 POC**：选 sale + mall，定义原子指标，试跑批量生成
3. **大模型调通**：推荐 + 口径生成 + 合理性检查
4. POC 通过后，全量铺开 14 个主题域

### 8.2 使用优先级

```
优先级 1：批量生成（覆盖高频常用组合）
  → 每个原子指标默认配 [本月][本年][环比][同比]
  → 预计覆盖 60-70% 的衍生指标需求

优先级 2：按需生成（临时需要时补充）
  → 日常维护中发现缺失的指标，即时生成
  → 预计覆盖 20-30%

优先级 3：手动定义（无法派生的复合指标）
  → 客单价 = 订单金额/订单数，需要手动定义计算逻辑
  → 预计 50-100 个
```

### 8.3 人工确认策略

```
批量生成后的人工确认 ≠ 逐个看

策略：
1. 大模型先做一轮自动评审（合理性检查）
2. 自动评审通过的 → 批量标记 approved
3. 有问题的 → 标记 pending，集中处理
4. 人工只看 pending 的，不看 approved 的

预估：1500 个衍生指标中，约 10-15% 需要人工介入
     → 人工确认量 ≈ 150-225 个，而非 1500 个
```

---

## 九、完整流程图

```
                    ┌──────────────┐
                    │  原子指标定义  │  (人工+大模型, ~150个)
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │  修饰词库     │  (预定义, ~30个)
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              ▼                         ▼
     ┌────────────────┐      ┌────────────────┐
     │  批量生成模式   │      │  按需生成模式   │
     │  原子×常用修饰  │      │  原子×指定修饰  │
     └───────┬────────┘      └───────┬────────┘
             │                        │
             ▼                        ▼
     ┌──────────────────────────────────────┐
     │          大模型自动处理                │
     │  · 命名生成（词根+缩写拼接）           │
     │  · 口径生成（模板+自然语言优化）       │
     │  · 合理性检查（业务逻辑校验）          │
     │  · 多模型比对                         │
     └──────────────┬───────────────────────┘
                    │
                    ▼
     ┌──────────────────────────────────────┐
     │          自动评审分流                  │
     │  · 合理 → auto approved (85-90%)     │
     │  · 有疑点 → pending (10-15%)         │
     └──────────────┬───────────────────────┘
                    │
                    ▼
     ┌──────────────────────────────────────┐
     │     人工批量确认（只看 pending）       │
     │  · 确认 → approved                    │
     │  · 修改 → 调整后 approved              │
     │  · 驳回 → rejected                    │
     └──────────────────────────────────────┘
```
