# 数据治理平台 — 功能缺失审查报告

> 审查日期：2026-08-05
> 审查范围：后端 API / CLI / 前端页面 / 测试 / 数据覆盖 / 工程化
> 依据：数据治理项目方案_v2.md、验收评分标准.md、指标批量生成机制.md、指标级质量评分体系.md

---

## 一、P0 核心功能缺失（影响交付，建议优先补齐）

### 1. 批量生成指标 = 「假功能」
- 前端 `page-batch-gen`（批量生成页）的 `batchGenerate()` **纯前端模拟**：`setTimeout(1200ms)` 后把预览行状态标成「✓ 已生成」，**不调用任何后端接口**，不写库。
- 后端无对应能力：无批量生成服务、无批量生成 API、CLI 无 `metric generate` 子命令（CLI 只有 `root generate` / `metric review`）。
- `指标批量生成机制.md`（原子指标 × 修饰词组合）只有设计，未落地。
- **影响**：这是把「数据从 4 条指标扩到 2000 条」的核心工具，缺失意味着平台无法自助扩容指标库。

### 2. 词根库只有读，没有写
- API 只有 `GET /api/roots`（可加 domain 过滤），**无创建/更新词根接口**。
- 词根是命名规范维度的基础字典，无法通过平台维护（只能手改 CSV）。
- 前端「词根库」页面应只能展示，无维护入口。

### 3. 血缘数据无上传通道
- 方案明确「血缘来源：外部项目解析后上传 JSON」，但 API **只有 `GET /api/lineage` / `/api/lineage/domains`，无上传端点**。
- 当前仅 `lineage/sale_lineage.json` 一个域有血缘文件（14 域要求），且 `column_mappings` 覆盖无法落地校验。

### 4. 指标/词根无删除能力
- API 列表无任何 `DELETE` 端点；指标「下线」只是 `review_status=offline`（PUT），不能真正删除误建指标。
- 误生成、误录入的数据只能手动清 CSV。

---

## 二、P1 管理能力缺口（发布与评审闭环）

### 5. 发布缺少质量门禁
- `publish_domain()` 只按 `review_status == approved` 过滤，**不校验评分等级**（如 grade < B 应禁止发布）。
- 建议：发布前校验该域指标平均分 / 单项等级，未达标阻止并返回原因。

### 6. 发布管理不完整
- 无**撤销/回滚发布**（发布后发现问题无法撤回版本号）；
- 无**跨域发布总览**（哪些域发布到哪一版、全库版本分布）；
- 无**版本间差异对比**（v2 → v3 改了哪些指标）。

### 7. 批量评分入口
- 后端有 `POST /api/scores/refresh`（全量重评），但前端是否提供「一键全量重评」按钮需确认（当前仅单指标「重新评分」）。

### 8. 命名/口径治理页疑似静态
- `page-naming` / `page-caliber` 由 app.js 用本地表格 + 分页渲染，**未见对应专用后端 API**，疑似对现有数据的前端二次加工，需人工验证是否真正反映评分引擎的违规清单（拼音残留、同名异义、口径空等）。

---

## 三、P2 质量与工程缺口

### 9. Task #17 未完成（测试 + 验证 + 文档）
- **scoring/ 与 release/ 零测试**：`tests/` 中无 `test_scoring*.py`、`test_release*.py`（仅 conftest 建了 scoring 目录）；
- **CLI 模块 0% 覆盖**；
- **LLM `parallel.py` / `factory.py` 覆盖率低**（约 36% / 32%）；
- 全量验证（pytest + ruff + mypy）与 overview 更新未收尾。

### 10. 两套评分体系并存，可能口径漂移
- `scoring/`（指标级六维度，`config/scoring_rules.json` 驱动）与 `acceptance/`（项目级 14 域验收，按验收评分标准 20/20/25/15/10/10 权重）**互不引用**。
- 同一指标可能有两套分数，验收报告未复用六维度评分结果，建议打通或明确各自定位。

### 11. 工程化收尾
- **CI/CD 缺失**：无 GitHub Actions（项目已上 GitHub，适合补）；
- **pre-commit 钩子未安装**（配置就绪，但 `.git/hooks/pre-commit` 不存在，提交未受 ruff/mypy/pytest 约束）；
- **mypy 残留 9 个错误**（cli.py pipeline 类型混用等历史问题）；
- 前端未 **ES module 化**（历史待办）。

---

## 四、P3 数据覆盖（供给问题，非代码缺失）

| 数据 | 现状 | 要求 | 缺口 |
|---|---|---|---|
| 主题域词根 | 3 域（base/cust/sale） | 14 域 × ≥10 词根 | 11 域空白 |
| 指标 | 4 条（仅 sale） | ~2000 条 | 差距巨大 |
| 血缘 | 1 域（sale） | 14 域 | 13 域空白 |

> 数据不足的根源与 P0-1 关联：**批量生成是假的 → 无法自助扩容 → 数据永远停在 demo 量级**。

---

## 五、优先级建议

| 优先级 | 事项 | 建议动作 |
|---|---|---|
| P0-1 | 批量生成指标后端 | 实现生成服务（原子×修饰词组合 + 校验 + 批量入库）+ `POST /api/metrics/batch-generate` + CLI `metric generate` + 前端接真接口 |
| P0-2/3 | 词根写 API、血缘上传 API | `POST/PUT /api/roots`、`POST /api/lineage/upload`（JSON 校验 + 落盘） |
| P1-5/6 | 发布门禁 + 撤销 + 版本对比 | release/service 加 grade 校验、`revert` 接口、`GET /api/releases`（跨域总览） |
| P2-9 | Task #17 | 补 scoring/release/CLI 测试，跑 pytest+ruff+mypy，更新 overview |
| P2-11 | CI + 钩子 | GitHub Actions（lint+test+coverage）、`pre-commit install`、清 mypy 历史错误 |

---
*本报告仅列缺口；已实现能力（六维度评分、多模型汇总、按域发布、验收引擎、24 个 API、12 个前端页面）不在本次范围。*
